# HouseholdHullabaloo

Requires node, npm, and docker or a local mongo installation.

Run through docker from root directory with docker compose up --build

For local development:
start local mongo service or docker compose up mongodb

Then in both the backend and frontend directories:
npm install
npm run dev
